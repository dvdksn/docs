---
title: docker compose watch
description: |
  Watch build context for service and rebuild/refresh containers when files are updated
sidebar:
  label: watch
---

<table class="cli-meta">
<tbody>
<tr><th>Description</th><td>Watch build context for service and rebuild/refresh containers when files are updated
</td></tr>
<tr><th>Usage</th><td><code>docker compose watch [SERVICE...]</code></td></tr>
</tbody></table>

## Description

Watch build context for service and rebuild/refresh containers when files are updated


## Options

| Option | Default | Description |
|--------|---------|-------------|
| `--no-up` |  | Do not build & start services before watching |
| `--prune` | `true` | Prune dangling images on rebuild |
| `--quiet` |  | hide build output |

